/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  basePath: '/cafe-julia',
  assetPrefix: '/cafe-julia',
};

export default nextConfig;